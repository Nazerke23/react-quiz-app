function Error() {
  return (
    <p className="error">
      <span>💥</span> There was an error fecthing questions. In order to app to
      work you need to install npm i json-server, after run command npm run
      server, and now you should be good. Hello again WORLD!!!
    </p>
  );
}

export default Error;
